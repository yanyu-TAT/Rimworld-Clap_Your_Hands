# C-04 已实现内容清单

> 记录已完成的功能、版本进度与待办。实现与文档不一致时，以 `Source/` `Defs/` 实际代码为准。

## 版本进度

| 版本 | 状态 | 内容 |
| --- | --- | --- |
| 0.0.1 | 开发中 | 项目初始化：工程骨架、编译链路、规范与文档体系 |

## 已实现

### 工程与基础设施

- [x] SDK 风格 csproj（`net472` + C# 10），最小引用集（`Assembly-CSharp` + `UnityEngine.CoreModule`）
- [x] 构建产物直出 `Clap_Your_Hands/Assemblies/Clap_Your_Hands.dll`，实测 0 警告 0 错误
- [x] `About.xml`（`yanyu.clapyourhands` / 1.6 / `modClass`）
- [x] 模组主类 `ClapYourHandsMod.cs`（原版 `Verse.Mod`，零依赖）
- [x] 清理脚本 `11.bat`、`.gitignore`
- [x] 规范体系：`AGENTS.md` + `docs/`（A/B/C 全套）+ `plans/击掌功能开发计划.md`

### 功能（v0.0.1 全部实际效果）

- [x] 击掌互动 Def（`Clap`）与触发权重（常数 2.0，好感度不影响权重）
- [x] 四档结果抽取与情绪 / 好感度（好感度经 `SimpleCurve` 修正概率）
- [x] 完美击掌的 12h 随机增益（3 级 + 灵感分支，灵感不可用时回退第三级）
- [x] 增益叠加规则（取最高 / 同级覆盖时长 / 低级忽略）
- [x] 24h 冷却（双方各记，OR 判定，代码留 `CooldownRequiresBoth` 开关）
- [x] 仇视排除（`opinion <= -20`，阈值与原版 `RivalOpinionThreshold` 一致）
- [x] 中英本地化（DefInjected + Keyed，逐键对称）
- [x] 调试指令（强制击掌 / 清除冷却 / 增减增益）
- [ ] 音效与特效（黑闪）—— 已推迟到 v0.0.2，见 `plans/击掌功能开发计划.md` 附录 A

## 待办 / 未决

- 音效素材来源（主人提供 / 程序化合成 / 先用原版占位）
- 「黑闪」文案的直白程度与特效强度
- 互动图标（`symbol`）是否自绘
- 游戏内实测：四档出现比例、增益数值、存读档后状态保持

## 美术与本地化现状

| 项 | 状态 |
| --- | --- |
| Textures | 暂无（`symbol` 暂用原版闲聊图标 `Things/Mote/SpeechSymbols/Chitchat`） |
| 中文简体 | 已完成（DefInjected 3 个 + Keyed 1 个） |
| English | 已完成（与中文逐键对称） |
